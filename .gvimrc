" Nicer font
"set guifont=Sauce_Code_Powerline:h10

" Copy, paste, etc.
"source $VIMRUNTIME/mswin.vim

" Set working directory to home
"cd ~

" My selected color scheme
colors desert

" Initial no. of lines to show
"set lines=40

" Enable auto-select to clipboard
"set guioptions+=a

" Disable GUI tab lines (it's ugly!)
"set guioptions-=e

" Hide toolbar
"set guioptions-=T

" Hide menu
"set guioptions-=m

" Place on upper left corner of screen
"winpos 0 0

